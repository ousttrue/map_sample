import { UserConfig } from "@ladle/react";
// import process from "node:process";

const config: UserConfig = {
  // viteConfig: process.cwd() + "/ladle-vite.config.ts",
  base: "/map_sample/",
};

export default config;
